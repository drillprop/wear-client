// Placeholder entry for apps/api — the fresh, empty scaffold from decision #28.
//
// The real server (GraphQL Yoga + Pothos + Drizzle on Node 24 / NodeNext ESM)
// arrives in phase 2 (#31). This module exists only so the workspace has a
// second app and the `typecheck` / `build` Turbo tasks are real and pass.
export const apiName = "@wear/api";
