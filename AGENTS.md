# AGENTS.md

## Agent skills

### Issue tracker

Issues and PRDs live in this repo's GitHub Issues (`drillprop/wear-client`), using the `gh` CLI. See `docs/agents/issue-tracker.md`.

### Triage labels

The five canonical triage roles, each mapped to a label string of the same name. See `docs/agents/triage-labels.md`.

### Domain docs

Multi-context: a root `CONTEXT-MAP.md` points at one `CONTEXT.md` per app (`apps/api`, `apps/web`). See `docs/agents/domain.md`.
