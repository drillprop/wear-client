import React from 'react';
import UserForm from '../../../UserForm/UserForm';

const EditNameForm: React.FC = () => {
  return <UserForm buttonText='save' onSubmit={() => {}}></UserForm>;
};

export default EditNameForm;
